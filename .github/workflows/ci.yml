name: CI
on:
  pull_request:
    branches: [develop, main]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "P5 replaces placeholder with frontend/backend build and tests"
